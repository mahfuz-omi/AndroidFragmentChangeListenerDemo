# AndroidFragmentChangeListenerDemo

This project has been created to get notified when the back stack of Fragment is changes, so we can perform some actions based on current Fragment (Top of Back Stack)
